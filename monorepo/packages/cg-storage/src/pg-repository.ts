import pg from'pg';import type{CGTimepoint,CGDomain,CGManifest,CGRelation,CGUASegment}from'cg-types/domain.js';import{Errors}from'cg-types/errors.js';
import type{ITimepointRepository,IDomainRepository,IManifestRepository,IRelationRepository,ISegmentRepository}from'./repository.js';
const bs=(v:bigint)=>v.toString();const tb=(v:unknown):bigint=>{if(typeof v==='bigint')return v;if(typeof v==='string')return BigInt(v);if(typeof v==='number')return BigInt(Math.round(v));return 0n;};
export class PgTimepointRepository implements ITimepointRepository{constructor(private p:pg.Pool){}async insert(tp:CGTimepoint):Promise<void>{await this.p.query(`INSERT INTO timepoints(machine_id,domain_name,domain_version,absolute_value,cgta,labels,created_at)VALUES($1,$2,$3,$4::NUMERIC,$5,$6::jsonb,$7::NUMERIC)ON CONFLICT DO NOTHING`,[tp.machine_id,tp.domain_name,tp.domain_version,bs(tp.absolute_value),tp.cgta,JSON.stringify(tp.labels),bs(tp.created_at)]);}async findById(id:string):Promise<CGTimepoint|null>{const r=await this.p.query('SELECT * FROM timepoints WHERE machine_id=$1',[id]);if(!r.rows[0])return null;const row=r.rows[0] as Record<string,unknown>;return{machine_id:row['machine_id'] as string,domain_name:row['domain_name'] as string,domain_version:row['domain_version'] as string,absolute_value:tb(row['absolute_value']),cgta:row['cgta'] as string,labels:(row['labels']??{}) as Record<string,string>,created_at:tb(row['created_at'])};}async list(l=100,o=0):Promise<CGTimepoint[]>{const r=await this.p.query('SELECT * FROM timepoints ORDER BY created_at DESC LIMIT $1 OFFSET $2',[l,o]);return r.rows.map((row:Record<string,unknown>)=>({machine_id:row['machine_id'] as string,domain_name:row['domain_name'] as string,domain_version:row['domain_version'] as string,absolute_value:tb(row['absolute_value']),cgta:row['cgta'] as string,labels:(row['labels']??{}) as Record<string,string>,created_at:tb(row['created_at'])}));}}
export class PgDomainRepository implements IDomainRepository{constructor(private p:pg.Pool){}async insert(d:CGDomain):Promise<void>{try{await this.p.query(`INSERT INTO domains(name,version,definition,published,created_at)VALUES($1,$2,$3::jsonb,$4,$5::NUMERIC)`,[d.name,d.version,JSON.stringify(d.definition),d.published,bs(d.created_at)]);}catch(e:unknown){if(e instanceof Error&&e.message.includes('unique'))throw Errors.SemanticError.duplicateName(`${d.name}@${d.version}`);throw e;}}async findByNameVersion(n:string,v:string):Promise<CGDomain|null>{const r=await this.p.query('SELECT * FROM domains WHERE name=$1 AND version=$2',[n,v]);if(!r.rows[0])return null;const row=r.rows[0] as Record<string,unknown>;return{name:row['name'] as string,version:row['version'] as string,definition:typeof row['definition']==='string'?JSON.parse(row['definition']):row['definition'] as CGDomain['definition'],published:row['published'] as boolean,created_at:tb(row['created_at'])};}async list():Promise<CGDomain[]>{const r=await this.p.query('SELECT * FROM domains ORDER BY created_at');return r.rows.map((row:Record<string,unknown>)=>({name:row['name'] as string,version:row['version'] as string,definition:typeof row['definition']==='string'?JSON.parse(row['definition']):row['definition'] as CGDomain['definition'],published:row['published'] as boolean,created_at:tb(row['created_at'])}));}async publish(n:string,v:string):Promise<void>{const r=await this.p.query(`UPDATE domains SET published=true,published_at=$1::NUMERIC WHERE name=$2 AND version=$3 AND published=false`,[bs(BigInt(Date.now())*1_000_000n),n,v]);if(r.rowCount===0)throw Errors.CGFSError.wormViolation(`${n}@${v}`);}}
export class PgManifestRepository implements IManifestRepository{constructor(private p:pg.Pool){}async insert(m:CGManifest):Promise<void>{await this.p.query(`INSERT INTO manifests(cgfi,tai_timepoint,content_hash,type_id,size_bytes,metadata,tombstone,created_at)VALUES($1,$2,$3,$4,$5::NUMERIC,$6::jsonb,$7,$8::NUMERIC)`,[m.cgfi,m.tai_timepoint,m.content_hash,m.type_id,bs(m.size_bytes),JSON.stringify(m.metadata),m.tombstone,bs(m.created_at)]);}async findByCGFI(cgfi:string):Promise<CGManifest|null>{const r=await this.p.query('SELECT * FROM manifests WHERE cgfi=$1',[cgfi]);if(!r.rows[0])return null;const row=r.rows[0] as Record<string,unknown>;return{cgfi:row['cgfi'] as string,tai_timepoint:row['tai_timepoint'] as string,content_hash:row['content_hash'] as string,type_id:row['type_id'] as string,size_bytes:tb(row['size_bytes']),tombstone:row['tombstone'] as boolean,metadata:(row['metadata']??{}) as Record<string,string>,created_at:tb(row['created_at'])};}async tombstone(cgfi:string):Promise<void>{const r=await this.p.query('UPDATE manifests SET tombstone=true WHERE cgfi=$1',[cgfi]);if(r.rowCount===0)throw Errors.CGFSError.fileNotFound(cgfi);}}
export class PgRelationRepository implements IRelationRepository{constructor(private p:pg.Pool){}async insert(r:CGRelation):Promise<void>{await this.p.query(`INSERT INTO relations(id,timepoint_a,timepoint_b,relation,computed_at)VALUES($1,$2,$3,$4,$5::NUMERIC)`,[r.id,r.timepoint_a,r.timepoint_b,r.relation,bs(r.computed_at)]);}async list():Promise<CGRelation[]>{const r=await this.p.query('SELECT * FROM relations ORDER BY computed_at DESC');return r.rows.map((row:Record<string,unknown>)=>({id:row['id'] as string,timepoint_a:row['timepoint_a'] as string,timepoint_b:row['timepoint_b'] as string,relation:row['relation'] as CGRelation['relation'],computed_at:tb(row['computed_at'])}));}}
export class PgSegmentRepository implements ISegmentRepository{constructor(private p:pg.Pool){}async allocate(g:string,sz:bigint,parent?:string):Promise<CGUASegment>{const base=await this._next();const{createHash}=await import('node:crypto');const id=createHash('sha256').update(`${g}:${base}:${sz}:${Date.now()}`).digest('hex').slice(0,16);const seg:CGUASegment={id,parent_id:parent??null,base_address:base,size_ns:sz,granted_by:g,status:'active',created_at:BigInt(Date.now())*1_000_000n};await this.p.query(`INSERT INTO segments(id,parent_id,base_address,size_ns,granted_by,status,created_at)VALUES($1,$2,$3::NUMERIC,$4::NUMERIC,$5,$6,$7::NUMERIC)`,[seg.id,seg.parent_id,bs(seg.base_address),bs(seg.size_ns),seg.granted_by,seg.status,bs(seg.created_at)]);return seg;}async resolve(id:string):Promise<CGUASegment>{const r=await this.p.query('SELECT * FROM segments WHERE id=$1',[id]);if(!r.rows[0])throw Errors.CGUASError.segmentNotFound(id);return this._r(r.rows[0] as Record<string,unknown>);}async revoke(id:string):Promise<void>{await this.p.query(`UPDATE segments SET status='revoked' WHERE id=$1`,[id]);}async list():Promise<CGUASegment[]>{const r=await this.p.query('SELECT * FROM segments ORDER BY base_address');return r.rows.map(row=>this._r(row as Record<string,unknown>));}private async _next():Promise<bigint>{const r=await this.p.query('SELECT MAX(base_address+size_ns) AS n FROM segments');return tb(r.rows[0]?.['n']??0);}private _r(r:Record<string,unknown>):CGUASegment{return{id:r['id'] as string,parent_id:r['parent_id'] as string|null,base_address:tb(r['base_address']),size_ns:tb(r['size_ns']),granted_by:r['granted_by'] as string,status:r['status'] as CGUASegment['status'],created_at:tb(r['created_at'])};}}
